# BindCraft 一次性代码

当前没有独立的一次性 Python 脚本。输入审计本身保存在 `bindcraft/notebooks/bindcraft_input_audit_20260826.ipynb`，因为它是需要人工阅读的审计产物。

未来报告构建器、输入迁移脚本或只运行一次的修复工具必须放在这里，并使用 `<动作><对象>_<YYYYMMDD>` 命名；正式原型或生产代码放在 `bindcraft/main/`。
