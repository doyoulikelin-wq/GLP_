# GLP_ 项目代理规则

## Windows 单机所有者模式

当仓库父目录存在 `WINDOWS_OWNER_MODE.json` 且其中 `status` 为 `ACTIVE` 时，本节优先于
`boltzgen/main/windows_gpu_handoff_20260829/` 中旧的 Mac/Windows 交接限制。

- Windows/WSL2 工作区是主工作区。Windows Codex 可以在本项目范围内读取、复制、创建、
  修改和执行源码、配置、环境、数据处理、现成权重推理、候选生成、筛选和 AI 评价任务。
- Windows Codex 可以自行建立分支、提交并推送代码；不需要 Mac Codex 复核、签发环境
  合同、接收补丁或批准经验事件。
- 旧文档中的 `PENDING_MAC_REVIEW`、`MAC_CODEX_REVIEW_REQUIRED`、必须回传 Mac、必须由
  Mac 冻结 CUDA 环境合同等要求，在所有者模式下均为历史兼容信息，不得阻塞执行。
- 原 T7 替换为本机 `LOCAL_ENV_ACCEPTANCE`：检查当前 GPU、CUDA、BF16、BoltzGen、原生
  GPU 算子和可用磁盘，记录实际环境后即可继续。无需双环境重建或版本化合同签发。
- 环境版本、批大小和采样预算可以根据 5070 Ti 的实测结果调整；必须把实际使用值写入
  本轮摘要，不得把调整后的探索运行冒充旧合同下的正式 G1/G2/AIV 通过。
- 成功和失败经验由 Windows 本地经验库直接接收。记录目的是帮助迭代，不是等待外部审批。

## 始终保留的技术边界

- 不训练、微调或改写 BoltzGen 模型权重；只使用现成权重推理。
- 原始输入和既有结果不覆盖；每次运行写入新的运行目录，禁止用 `--reuse` 混入旧结果。
- 保留普通命令日志、GPU 错误、失败原因和实际参数；检查缺失文件、空结构、NaN/Inf、候选数
  和要求的复折叠样本数。
- 运行前后确认实际使用的权重文件未被修改；同一时间只运行一个 GPU 主任务。
- 计算结果不得表述为实验结合、亲和力、选择性、安全性或成药性结论。
- 不把权重、大型原始数据、完整运行结果、访问令牌、私钥、`.env` 或个人材料提交到公开
  GitHub。
- 未经用户明确要求，不删除源数据、不覆盖历史结果、不做工作区外的不可恢复操作。

## 保留对照数据

Windows 可以持有并读取全部已交付数据。`peptide_lockbox_countertargets_20260823` 若在候选
和阈值确定前用于调参，就不再是盲态锁箱，只能标记为开发负对照；以后若需要无偏 AIV4，
必须另建从未用于调参的新保留集合。这是结果解释规则，不是 Mac 授权门。
