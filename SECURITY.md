# 安全与敏感信息

## 不要提交

- API token、SSH 密钥、云凭据、带凭据 URL 或 `.env`；
- 姓名、邮箱、赛事报名表、盲法映射和未公开实验批次；
- 未授权候选序列或受限制第三方数据；
- 模型权重或缓存；它们既可能过大，也可能受独立许可约束。

## 提交前检查

运行：

```bash
python3 shared/main/repository_policy/check_repository_policy.py
```

该检查覆盖禁止扩展、文件大小、一次性代码和数据目录命名、绝对本机路径以及常见凭据模式。它不能替代人工审查。

## 发现问题

不要在公开 issue 中粘贴 secret 或敏感数据。停止进一步推送，立即轮换相关凭据，并通过仓库所有者的私有渠道报告精确路径和提交。是否重写 Git 历史需要单独授权和备份，不可直接 force-push。
