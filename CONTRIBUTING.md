# 贡献与目录规范

## 分支和审阅

1. 从 `main` 建功能分支，不直接向 `main` 提交。
2. 所有变更经 Pull Request；首次或高风险数据变更先开 draft。
3. 不覆盖历史运行目录。修正或重跑必须创建新的 `<尝试内容>_<YYYYMMDD>` 包。
4. 提交前运行 `python3 shared/main/repository_policy/check_repository_policy.py`。

## 主代码与一次性代码

- 可复用、被正式流程调用或持续维护的代码放在对应路线的 `main/`。
- 只为一次报告、Notebook、迁移、剖析或封存服务的代码必须放在 `tools/one_off/`。
- `tools/one_off/` 脚本不得被正式流程导入；文件头要写明输入、输出、副作用和退出条件。
- 一次性脚本执行完仍保留，作为产物来源证据；若不再可运行，在同目录 README 标记 `ARCHIVED`，不要无说明删除。
- 不创建名为 `boltzgen.py` 或 `bindcraft.py` 的本地顶层模块，以免遮蔽上游包。

## 命名

`*` 只表示语义分隔，不写入真实文件名；真实分隔符统一为 `_`。

- 尝试目录或公开执行入口：`<尝试内容>_<YYYYMMDD>`。
- 数据目录：`<用处>_<YYYYMMDD>`；需要区分同日快照时用 `<用处>_<YYYYMMDD_HHMMSS>`。
- HTML、Notebook 和计划：`<主题>_<YYYYMMDD>.<ext>`。
- 一次性脚本：`<动作><对象>_<YYYYMMDD>.py`，且位于 `tools/one_off/`。
- 不使用 `latest`、`new`、`final_v2`、`copy` 或无日期的 `run_001` 作为仓库规范名。
- 已冻结历史包内部若存在相对路径合同，可保留原实现文件名；必须在包根提供符合上述规则的带日期入口，并在 README 解释映射。

## 数据进入仓库的条件

每个数据资源至少记录：用途、来源、条目或 revision、获取/创建时间、格式、粒度、记录数、文件数、字节数、SHA-256 或校验清单、许可、敏感级别、Git 策略、消费者、验证状态和限制。

以下文件不能进入 Git 或 Git LFS：模型权重、完整缓存、环境、原始大型结构库、完整 runs/logs/NPZ、凭据、个人信息、盲法映射、未授权候选序列和未经许可审查的第三方源码副本。

小型公开结构输入只有在来源、许可、派生方式和哈希全部登记后才能提交。

## 代码和报告要求

- Python 文件需要模块文档字符串、关键函数说明以及对非显然步骤的注释。
- 入口需要明确输入、输出、运行环境和科学边界；不得把推理称为训练。
- HTML 必须自包含或明确声明依赖；报告需保留规范 artifact 和来源索引。
- 结果必须区分：工程完成、数据完整、计算过滤通过和实验验证。
- 计算代理不得写成解离常数、结合真值、选择性真值或临床结论。

## 第三方和敏感信息

- 上游代码用 tag/commit/URL 固定，不复制 vendor 树；确需复制时必须同时保留许可证和版权声明。
- 模型、数据和软件依赖分别审查许可，不能用源码许可证推定权重或数据许可。
- 提交前搜索绝对本机路径、邮箱、姓名、token、密钥和带凭据 URL。
- 发现敏感信息时停止推送；若已进入历史，先轮换凭据，再按仓库安全流程处理历史。
