# VHH 输入兼容性修复与端部条件对照：2026-09-11

## 本轮任务与边界

用户要求先解决输入与评价兼容性，再做端部识别小型受控对照。
本轮选择严格全原子评价，新增运行前 CPU 检查；不通过补标缺失原子来放行输入。
不修改 2026-09-10 的冻结代码、失败结果及事后局部诊断。
不增加骨架，不训练或改写权重，不自动重试、不转 BindCraft。

## 运行前固定的研究问题

在同一个来源完整的 7XL0 骨架、相同目标、相同 CDR 设计范围、
相同 adherence 权重与预算下，改变端部条件，观察生成与自由复折叠的端部接触。

| 条件 | His7/Ala8 | 目标其余 28 个残基 |
|---|---|---|
| A：无位置提示 | 未指定 | 未指定 |
| B：仅提示端部 | BINDING | 未指定 |
| C：端部提示及其余位置排除 | BINDING | NOT_BINDING |

NOT_BINDING 是模型的软条件，不是硬性禁止接触。
固定预算为每组 2 条候选、每条 5 次自由复折叠，共 6 条候选、30 次预测；
单个 GPU 主任务串行执行，GPU 工作流硬上限 1800 秒。
无论结果好坏均保留所有候选及其分母；软件运行成功不等于真实结合。

主要描述为：生成构象双端部接触、自由复折叠双端部接触次数、
每候选全部 5 次保持双端部接触。姿态 V2 仅作有适用范围的补充诊断。
不以旧过滤幸存数代替这三个指标，不用姿态 N/A 充当零类或不能结合。

## 中间过程

- 截至 01:34（北京时间）：已恢复本机项目状态与上轮停止条件；工作树干净，
  上轮提交为 `5d916f8`。并行检查输入兼容性、三条件语法与严格汇总。
  此阶段未启动 GPU；具体工具日志保留在本任务。
- 已确认第三种条件可由上游解析：`binding: 1..2` 与
  `not_binding: 3..30`。不能对全部残基设置 not_binding，否则会覆盖端部提示。
- 已识别随机性限制：上游数据处理存在独立熵源，不能仅凭通常的全局 seed
  声称三组共享完全相同的随机数。CPU 条件对照中的固定随机环境只用于核对输入，
  不等同于实际 GPU 全链确定性。本轮按等预算随机探索解释，
  不把两个候选的组间差异当作确定因果、统计显著性或结合优势。

后续实际检查、冻结计划、运行耗时、失败和结果在本文件追加；不倒填为事前证据。

## 运行前发现并修复的问题

1. 实际 CPU 双表示检查：7XL0、6APO 全原子兼容；8IM0 的生成表示及
   规范源表示均检出 16 个缺失框架原子，返回拒绝。检查没有修改源坐标、解析标记或旧 E 评价器。
2. 输入合格不保证尚未生成的 CDR 也合格。新运行器在每组结束后，立即对真实生成结构
   和 5 次复折叠运行原严格评价；出现不兼容就停止，不继续消耗后续组 GPU。
3. CPU 证据核对同时检查三条件的真实解析／掩码标签、特征快照摘要、原始四文件和上游
   相关源码摘要。仅有 READY 标签、同长度骨架或未变的 JSON 文件不构成放行依据。
4. A 组无指定结合位点，上游分析不输出旧的位点 RMSD 筛选列；旧筛选器访问该列会
   `KeyError`。该路径已用 A 组真实 CPU 特征和现有筛选逻辑复现，尚未消耗 GPU。
   新的执行适配器对 **三组统一关闭旧 binding-site 筛选**，
   同时将配置验收要求固定为关闭；其余执行、锁、超时、权重校验和收据流程保留。
   端部接触仍由独立严格 E 指标统一计算，不给缺列补零、不改原始旧脚本。

CPU 固定随机环境下，三组每个样本的 78 个非 binding 张量一致；这只支持
输入条件隔离检查，不证明未固定随机源的实际 GPU 派生张量或结果也相同。

## GPU 运行前冻结

- [实际输入兼容性结果](reports/vhh_terminal_control_20260911/CPU_INPUT_COMPATIBILITY.json)：
  两个完整来源通过，8IM0 拒绝；新三条件均在冻结时再次真实解析并通过。
- [三条件实际 CPU 准备](reports/vhh_terminal_control_20260911/CPU_CONDITION_PREPARATION.json)。
- [软件测试与开发中失败记录](reports/vhh_terminal_control_20260911/PRE_GPU_TEST_SUMMARY.json)：
  最终 358 项通过、1 个既有依赖弃用提示；约 29.15 秒。
  开发中第一次回归有 4 个未适配新门的测试失败，已保留，不算 GPU 失败。
- [运行前固定计划](reports/vhh_terminal_control_20260911/PREREGISTRATION.json)，私有计划摘要
  `3633291b9d4e22b2b7d68fda4f45b24f5e320c399a8c4f0d576e0f772d91a1e4`。

全仓政策检查仍发现历史测试说明和被忽略的缓存目录问题；这些文件不加入提交。
本轮交付另按实际改动文件检查，不能称为全仓政策检查通过。

## A 组执行停止及 CPU 补验证

固定方案先以 `7479249` 提交并推送。A 组实际完成生成、逆折叠、10 次自由复折叠、
分析和筛选，各阶段退出码均为 0；最终旧输出检查器仍要求
`bindsite_under_8rmsd` 列，因 A 组无指定结合位置而失败。
这说明运行前只修复旧筛选器还不够，遗漏了下游旧输出检查器同样的必填列要求。

原尝试自动停止于约 198.66 秒，未启动 B/C；原 `EXECUTION_FAILED`、
原终态收据、报错和输出全部保留，见[首次尝试失败记录](reports/vhh_terminal_control_20260911/FIRST_ATTEMPT_FAILURE.json)。
不将此次过程描述为“首次无错误跑完”，也不重复 A 的 GPU 生成。

随后对 A 组进行了独立只读结构核验：两候选分别 1149、1135 个建模原子，
生成及每候选五次复折叠全部可按原 E 严格评价；每候选 91 个非 CDR 框架残基与
来源逐位一致，目标身份、30 个 CDR 位置和全零 binding 标签正确。
此时未用 A 的接触结果选择新条件或修改评价阈值。

补救限定为旧输出列的兼容性：新 CPU 包装器将旧位点 RMSD 列改为可选；
若存在仍执行原有限值检查，缺失记为不适用，不补零、不改原文件。
其他输出数量、序列、原子映射、结构、有限值及五次复折叠检查仍由原检查逻辑执行，
并继续使用已冻结的 E 接触评价。

只有新的 CPU 补验证及严格结构检查均通过，才以新的执行计划继续原先未启动的 B/C。
原三条件、六候选总预算及接触定义不变；截止仍为首次开始时间加 1800 秒，
包含本次 CPU 修复间隔，不重新获得 30 分钟。

02:00 前，CPU 补验证通过，续跑相关回归最终 403 项通过（28.92 秒，1 个既有警告）。
[续跑冻结计划](reports/vhh_terminal_control_20260911/CONTINUATION_PREREGISTRATION.json)
只允许 B/C 各运行一次；A 的原始失败状态保留，旧可选列仅记不适用。
绝对截止为北京时间 02:13:32；本次修复间隔计入，不延长。

## 最终完成与结果

- 续跑代码和计划以 `c50784b` 冻结并推送后执行；A 未重跑。
- B 于北京时间 01:59:27 开始，C 于 02:02:37 开始；均退出 0，输出检查和严格全原子评价通过。
- 02:05:40.961 完成全部计算。首次开始至此 1328.73 秒（22 分 8.73 秒），
  包括中途修复／测试／发布空档，未超过原 1800 秒截止。
- 原冻结严格汇总成功，用时约 0.44 秒；全部 6 条候选、30 次自由复折叠可评价，
  六条序列互异，每条 91 个非 CDR 框架残基均与来源逐位相同，无局部原子降级。

| 条件 | 生成双端接触 | 自由复折叠双端接触 | 5/5 持续双端接触候选 |
|---|---:|---:|---:|
| A：无位置提示 | 0/2 | 1/10 | 0/2 |
| B：仅端部提示 | 0/2 | 0/10 | 0/2 |
| C：端部提示＋其余位置非结合软条件 | 0/2 | 0/10 | 0/2 |

三组没有稳定 5/5 双端部接触候选。A 的单次接触不能证明优于 B/C；五次复折叠
不是五条独立分子，样本量和随机源限制不允许统计排名或确定因果。
C 组生成时端部更靠近 CDR，但仍未达到 4.5 Å 双接触定义，未因接近阈值而改判。
自由复折叠姿态 V2 为 N/A，不替代端部接触，也不解释为不能结合。

公开结果、运行问题、复现入口和下一步停止状态见
[结果索引](reports/vhh_terminal_control_20260911/README.md)。
当前停止追加 GPU；不扩骨架、不转 BindCraft、不宣称活性／截短选择性或实验结合。

独立核验直接从原始 NPZ 用重原子距离复算，未调用接触汇总函数，三组计数一致；
另核对全部六条框架、目标身份及实际 binding 标签。来源摘要和方法见
[独立 CPU 复核](reports/vhh_terminal_control_20260911/INDEPENDENT_CPU_RECHECK.json)。
分析验证流程要求区分缺失指标与零、重复预测与独立分子，以及原始失败与补验通过，
因此这些限制均在结果中显式保留。

## 时间账本

| 阶段命令（秒） | A | B | C | 合计 |
|---|---:|---:|---:|---:|
| 配置及配置检查 | 5 | 5 | 5 | 15 |
| 结构设计 | 55 | 57 | 54 | 166 |
| 序列设计 | 12 | 14 | 13 | 39 |
| 自由复折叠 | 71 | 69 | 69 | 209 |
| 分析 | 23 | 22 | 23 | 68 |
| 筛选 | 13 | 13 | 13 | 39 |
| 输出验证 | 1（原失败） | 2 | 1 | 4 |
| 合计 | 180 | 182 | 178 | 540 |

首次 GPU 尝试至最后结束：540 秒已计时阶段（40.64%）＋748.47 秒两次执行间的
修复／CPU 补验／测试／冻结发布空档（56.33%）＋40.26 秒其他未单项计时执行开销
（3.03%）＝1328.73 秒。阶段命令包括加载和 CPU 工作，不等于纯 GPU 内核时间；
也没有依据把其他开销都算作哈希检查。窗口不含此前开发和此后结果汇总／最终推送。
完整精度、时间边界和限制见 [时间记录](reports/vhh_terminal_control_20260911/PUBLIC_TIMING.json)。

最终收尾检查：本次 8 个收尾文件的文件政策、JSON 解析及 Markdown 本地链接检查通过；
公开汇总与本机原导出摘要一致，独立复算计数、各组分母和时间账本加法核对通过。
未提交序列或结构原始数据；既有全仓政策问题仍单独保留。
